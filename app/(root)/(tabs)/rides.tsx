import { View, Text, SafeAreaView } from "react-native";

export const Rides = () => {
  return (
    <SafeAreaView className={"bg-white flex-1"}>
      <View>
        <Text className={"text-2xl text-green-700 text-center"}>RIDES</Text>
      </View>
    </SafeAreaView>
  );
};

export default Rides;
